---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: by-dependencies
  labels:
    name: by-dependencies
  namespace: sock-shop-by-dependencies
spec:
  replicas: 1
  selector:
    matchLabels:
      name: by-dependencies
  template:
    metadata:
      labels:
        name: by-dependencies
    spec:
      containers:
      - name: by-dependencies
        image: buzatof/by-dependencies
        env:
         - name: ZIPKIN
           value: zipkin.jaeger.svc.cluster.local
         - name: JAVA_OPTS
           value: -Xms64m -Xmx128m -XX:+UseG1GC -Djava.security.egd=file:/dev/urandom -Dspring.zipkin.enabled=false
        resources:
          limits:
            cpu: 500m
            memory: 1000Mi
          requests:
            cpu: 100m
            memory: 300Mi
        ports:
        - containerPort: 80
        securityContext:
          runAsNonRoot: true
          runAsUser: 10001
          capabilities:
            drop:
              - all
            add:
              - NET_BIND_SERVICE
          readOnlyRootFilesystem: true
        volumeMounts:
        - mountPath: /tmp
          name: tmp-volume
      volumes:
        - name: tmp-volume
          emptyDir:
            medium: Memory
      nodeSelector:
        beta.kubernetes.io/os: linux
        namespace: sock-shop-by-dependencies
