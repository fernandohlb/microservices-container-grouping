---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: front-end
  labels:
    name: front-end
    app: front-end
    product: by-dependencies
  namespace: by-dependencies
spec:
  replicas: 1
  selector:
    matchLabels:
      name: front-end
  template:
    metadata:
      labels:
        name: front-end
        department: by-dependencies
    spec:
      containers:
      - name: front-end
        image: buzatof/front-end
        resources:
          limits:
            cpu: 1000m
            memory: 1100Mi
          requests:
            cpu: 100m
            memory: 300Mi
        ports:
        - containerPort: 8079
        env:
        - name: SESSION_REDIS
          value: "true"
        securityContext:
          runAsNonRoot: true
          runAsUser: 10001
          capabilities:
            drop:
              - all
          readOnlyRootFilesystem: true
        readinessProbe:
          httpGet:
             path: /metrics
             port: 8079
          periodSeconds: 30
          timeoutSeconds: 120
          failureThreshold: 10
          initialDelaySeconds: 30
        livenessProbe:
          httpGet:
            path: /metrics
            port: 8079
          initialDelaySeconds: 300
          timeoutSeconds: 120
        # readinessProbe:
        #   httpGet:
        #     path: /metrics
        #     port: 8079
        #   initialDelaySeconds: 180
          # timeoutSeconds: 3
      nodeSelector:
        kubernetes.io/os: linux
        namespace: by-dependencies
      tolerations:
        - key: "namespace"
          operator: "Equal"
          value: "by-dependencies"
          effect: "NoSchedule"
